import React from 'react'
import { shallow } from 'enzyme'
import StoreId from '../pages/stores/[storeId]'

describe("StoreView", () => {
    const wrap = shallow(<StoreId />)

    // click on a item, itemcard should appear
    it("should workkkkk")
})